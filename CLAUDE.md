# telegram-sessions

## Plugin deployment

Update the plugin in Claude Code after making changes:

```bash
claude plugin update telegram-sessions@telegram-sessions-marketplace
```

This refreshes the cached plugin in `~/.claude/plugins/cache/`.
